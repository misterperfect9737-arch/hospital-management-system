# VIVA COMMANDS - COPY AND PASTE THESE

========================================
STEP 1: NAVIGATE TO PROJECT
========================================
cd "C:\hosipital final"

========================================
STEP 2: INSTALL DEPENDENCIES
========================================
python -m pip install -r requirements.txt

If that doesn't work, try:
py -m pip install -r requirements.txt

========================================
STEP 3: RUN APPLICATION
========================================
python app.py

If that doesn't work, try:
py app.py

========================================
STEP 4: OPEN BROWSER
========================================
http://localhost:5000
OR
http://127.0.0.1:5000

========================================
LOGIN CREDENTIALS
========================================
Admin:
Username: admin
Password: admin123

Doctor:
Username: dr.smith
Password: doctor123

Patient:
Register new account during demo

========================================
TO STOP APPLICATION
========================================
Press: Ctrl + C in CMD window

========================================
OPTIONAL: RUN CHECKSUM (if required)
========================================
pip install checksumdir
python check.py
